apiVersion: v1
kind: Service
metadata:
  name: {{ include "mqconnector.fullname" . }}
  labels:
    {{- include "mqconnector.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: https
      port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
  selector:
    {{- include "mqconnector.selectorLabels" . | nindent 4 }}
